apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-microservice
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-microservice
  template:
    metadata:
      labels:
        app: demo-microservice
    spec:
      containers:
      - image: nginx:latest
        name: demo-microservice
        ports:
        - containerPort: 80
        resources:
          limits:
            memory: "5Mi"
            cpu: "50m"
          requests:
            memory: "5Mi"
            cpu: "10m"
